name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x]
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
      - name: Install root dependencies (including dev deps)
        run: pnpm install --frozen-lockfile
      - name: Lint backend
        run: pnpm --workspace @job-assistant/backend run lint
      - name: Lint frontend
        run: pnpm --workspace @job-assistant/frontend run lint
      - name: Test backend
        run: pnpm --workspace @job-assistant/backend run test
      - name: Test frontend
        run: pnpm --workspace @job-assistant/frontend run test
      - name: Build backend
        run: pnpm --workspace @job-assistant/backend run build
      - name: Build frontend
        run: pnpm --workspace @job-assistant/frontend run build
